# Cold-plunge example

Two isolated SKU configurations are included:

- `product-oval.yaml`
- `product-round.yaml`

Do not combine references from different tub variants in one run. Add your own product photographs to `references/` using the filenames listed in `references/README.md`. Clean front and angle photographs should be the primary machinery references; supplier composites should be used only as shape or style references.

The chiller dimensions configured in the examples are 470 × 520 × 630 mm. Confirm every product fact before publishing generated assets.
